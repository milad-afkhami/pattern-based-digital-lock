> **[🇬🇧 English Version](README.md)**

# فایل‌های شبیه‌سازی (`simulation/`)

این دایرکتوری فایل‌های شکل موج تولید شده در طول شبیه‌سازی را ذخیره می‌کند.

---

## محتویات

پس از اجرای تست‌بنچ‌ها، این دایرکتوری حاوی فایل‌های شکل موج `.ghw` است:

| فایل | تست‌بنچ منبع |
|------|--------------|
| `tb_digital_lock.ghw` | تست‌های واحد FSM |
| `tb_top_level.ghw` | تست‌های سیستم کامل |
| `tb_fsm_coverage.ghw` | تست‌های پوشش |
| `tb_edge_cases.ghw` | تست‌های شرایط مرزی |
| `tb_debouncer.ghw` | تست‌های debouncer |

<details>
<summary>فایل‌های .ghw چیست؟</summary>

فایل‌های GHW (شکل موج GHDL) حاوی مقادیر سیگنال ضبط شده در طول شبیه‌سازی هستند. آنها مانند یک "جعبه سیاه" هستند که هر تغییر سیگنال را ضبط می‌کنند و به شما امکان تحلیل آنچه در طول تست اتفاق افتاده را می‌دهند.

</details>

---

## مشاهده شکل موج‌ها

### باز کردن با GTKWave

```bash
# مشاهده هر فایل شکل موج
gtkwave tb_digital_lock.ghw

# یا از ریشه پروژه
gtkwave simulation/tb_digital_lock.ghw
```

### راهنمای سریع GTKWave

1. **افزودن سیگنال‌ها**:
   - پنل چپ: سلسله مراتب را باز کنید (روی `+` کلیک کنید)
   - روی نام سیگنال‌ها دوبار کلیک کنید تا اضافه شوند

2. **ناوبری**:
   - چرخ ماوس: بزرگنمایی/کوچکنمایی
   - کلیک روی خط زمان: جابجایی مکان‌نما
   - Ctrl+کلیک: گذاشتن نشانگر

3. **میانبرهای مفید**:
   - `+` / `-`: بزرگنمایی
   - `Home`: رفتن به ابتدا
   - `End`: رفتن به انتها
   - `Left`/`Right`: جابجایی مکان‌نما

<details>
<summary>تنظیم پیشنهادی سیگنال برای اشکال‌زدایی</summary>

برای اشکال‌زدایی قفل دیجیتال، این سیگنال‌ها را اضافه کنید:

```
tb_digital_lock
├── clk
├── reset
├── button_A
├── button_B
├── button_C
├── button_D
├── lock_status
└── uut (باز کردن)
    ├── current_state
    ├── next_state
    ├── unlock_timer
    └── timer_expired
```

چیدمان سیگنال را ذخیره کنید: File → Write Save File

</details>

---

## تولید شکل موج‌ها

شکل موج‌ها با فلگ `--wave` تولید می‌شوند:

```bash
ghdl -r --std=08 tb_digital_lock --wave=simulation/tb_digital_lock.ghw
```

### گزینه‌های خروجی دیگر

```bash
# فرمت VCD (جهانی‌تر، فایل‌های بزرگ‌تر)
ghdl -r --std=08 tb_digital_lock --vcd=simulation/tb_digital_lock.vcd

# فرمت FST (فشرده، مناسب برای طراحی‌های بزرگ)
ghdl -r --std=08 tb_digital_lock --fst=simulation/tb_digital_lock.fst
```

---

## پاک‌سازی

فایل‌های شکل موج می‌توانند بزرگ باشند. برای پاک‌سازی:

```bash
# حذف همه فایل‌های شکل موج
rm simulation/*.ghw
rm simulation/*.vcd
rm simulation/*.fst
```

---

## اندازه فایل‌ها

اندازه‌های معمول برای این پروژه:

| تست | مدت | اندازه فایل |
|-----|-----|-------------|
| tb_digital_lock | ~1μs | ~50 KB |
| tb_top_level | ~5μs | ~200 KB |
| tb_edge_cases | ~10μs | ~500 KB |

<details>
<summary>کاهش اندازه فایل‌ها</summary>

برای شبیه‌سازی‌های طولانی، اندازه فایل‌ها می‌تواند بزرگ شود. گزینه‌ها:

1. **شبیه‌سازی‌های کوتاه‌تر**: از مقادیر زمانی کوچک‌تر استفاده کنید
2. **سیگنال‌های کمتر**: از `--wave` فقط با سیگنال‌های خاص استفاده کنید
3. **فرمت FST**: فشرده‌تر از GHW/VCD
4. **ذخیره نکنید**: اگر نیاز ندارید `--wave` را رد کنید

</details>

---

## توجه

این دایرکتوری معمولاً در `.gitignore` است زیرا فایل‌های شکل موج:
- تولید شده هستند، نه کد منبع
- می‌توانند بزرگ باشند
- با اجرای شبیه‌سازی‌ها به راحتی دوباره تولید می‌شوند
