> **[🇬🇧 English Version](README.md)**

# اسکریپت‌ها (`scripts/`)

اسکریپت‌های شل برای ساخت، تست و مدیریت پروژه قفل دیجیتال.

---

## مرجع سریع

| اسکریپت | هدف | استفاده رایج |
|---------|-----|--------------|
| `install.sh` | نصب GHDL و GTKWave | `./scripts/install.sh` |
| `build.sh` | کامپایل فایل‌های منبع | `./scripts/build.sh` |
| `test.sh` | اجرای تست‌بنچ‌ها | `./scripts/test.sh` |
| `synth.sh` | بررسی قابلیت سنتز | `./scripts/synth.sh` |
| `wave.sh` | باز کردن نمایش‌دهنده شکل موج | `./scripts/wave.sh tb_digital_lock` |
| `clean.sh` | حذف فایل‌های تولید شده | `./scripts/clean.sh` |

---

## استفاده تفصیلی

### install.sh

نصب GHDL (شبیه‌ساز) و GTKWave (نمایش‌دهنده شکل موج).

```bash
# تشخیص خودکار سیستم‌عامل و نصب
./scripts/install.sh
```

سیستم‌های پشتیبانی شده:
- Ubuntu/Debian (apt)
- Fedora (dnf)
- Arch Linux (pacman)
- macOS (Homebrew)

---

### build.sh

کامپایل همه فایل‌های منبع VHDL به ترتیب وابستگی صحیح.

```bash
# ساخت معمولی
./scripts/build.sh

# ساخت تمیز (ابتدا حذف artifacts قدیمی)
./scripts/build.sh --clean
```

---

### test.sh

اجرای یک یا همه تست‌بنچ‌ها با تولید شکل موج اختیاری.

```bash
# اجرای همه تست‌ها (با شکل موج)
./scripts/test.sh

# اجرای همه تست‌ها (سریع‌تر، بدون شکل موج)
./scripts/test.sh --no-wave

# اجرای تست‌بنچ خاص
./scripts/test.sh tb_digital_lock
./scripts/test.sh tb_top_level
./scripts/test.sh tb_fsm_coverage
./scripts/test.sh tb_edge_cases
./scripts/test.sh tb_debouncer

# اجرای تست‌بنچ خاص بدون شکل موج
./scripts/test.sh tb_digital_lock --no-wave
```

---

### synth.sh

تأیید اینکه طراحی می‌تواند برای FPGA سنتز شود.

```bash
./scripts/synth.sh
```

این همه کامپوننت‌ها را قبل از صرف وقت در ابزارهای تولیدکننده برای مشکلات قابلیت سنتز بررسی می‌کند.

---

### wave.sh

باز کردن GTKWave برای مشاهده شکل موج‌های شبیه‌سازی.

```bash
# مشاهده پیش‌فرض (tb_digital_lock)
./scripts/wave.sh

# مشاهده تست‌بنچ خاص
./scripts/wave.sh tb_top_level
./scripts/wave.sh tb_fsm_coverage
```

توجه: ابتدا `test.sh` را برای تولید فایل‌های شکل موج اجرا کنید.

---

### clean.sh

حذف فایل‌های تولید شده.

```bash
# حذف artifacts ساخت (نگه داشتن شکل موج‌ها)
./scripts/clean.sh

# حذف همه چیز شامل شکل موج‌ها
./scripts/clean.sh --all
```

---

## گردش کار معمول

```bash
# 1. راه‌اندازی بار اول
./scripts/install.sh

# 2. ساخت پروژه
./scripts/build.sh

# 3. اجرای همه تست‌ها
./scripts/test.sh

# 4. مشاهده شکل موج‌ها در صورت نیاز به اشکال‌زدایی
./scripts/wave.sh tb_digital_lock

# 5. بررسی قابلیت سنتز قبل از کار FPGA
./scripts/synth.sh

# 6. پاک‌سازی پس از اتمام
./scripts/clean.sh
```

---

## کدهای خروج

همه اسکریپت‌ها از کدهای خروج استاندارد استفاده می‌کنند:
- `0` - موفقیت
- `1` - خطا (وابستگی گمشده، شکست تست و غیره)

این آنها را مناسب خطوط لوله CI/CD می‌کند:

```bash
./scripts/test.sh || echo "Tests failed!"
```

---

## اجرایی کردن اسکریپت‌ها

اگر خطای "permission denied" دریافت کردید:

```bash
chmod +x scripts/*.sh
```

یا مستقیماً با bash اجرا کنید:

```bash
bash scripts/test.sh
```
