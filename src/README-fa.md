> **[🇬🇧 English Version](README.md)**

# فایل‌های منبع (`src/`)

این دایرکتوری حاوی فایل‌های منبع VHDL قابل سنتز برای قفل دیجیتال مبتنی بر الگو است.

---

## فایل‌ها

| فایل | خطوط | توضیحات |
|------|------|---------|
| [digital_lock.vhd](digital_lock.vhd) | ۱۵۶ | کنترلر اصلی FSM |
| [button_debouncer.vhd](button_debouncer.vhd) | ۹۳ | مدار حذف نویز دکمه |
| [top_level.vhd](top_level.vhd) | ۱۵۴ | یکپارچه‌سازی سیستم |

---

## ترتیب کامپایل

فایل‌ها باید به ترتیب وابستگی کامپایل شوند:

```bash
# ۱. ابتدا ماژول‌های مستقل
ghdl -a --std=08 digital_lock.vhd
ghdl -a --std=08 button_debouncer.vhd

# ۲. top-level در آخر (وابسته به هر دو بالا)
ghdl -a --std=08 top_level.vhd
```

<details>
<summary>چرا ترتیب مهم است؟</summary>

VHDL به یک "work library" کامپایل می‌شود. وقتی یک فایل به کامپوننت دیگری ارجاع می‌دهد، آن کامپوننت باید قبلاً در کتابخانه وجود داشته باشد. اگر به ترتیب نادرست کامپایل کنید، خطاهایی مانند زیر می‌بینید:

```
error: cannot find entity work.digital_lock
```

</details>

---

## سلسله مراتب ماژول‌ها

```
top_level
├── button_debouncer (×4 نمونه)
│   └── فیلتر نوسان دکمه، ارائه پالس‌های تمیز
└── digital_lock (×1 نمونه)
    └── کنترلر FSM، مدیریت حالت قفل
```

---

## مرجع سریع

### digital_lock.vhd

**هدف**: FSM پنج‌حالته پیاده‌سازی منطق توالی باز کردن

**پورت‌ها**:
- `clk`, `reset` - کلاک و ریست
- `button_A/B/C/D` - ورودی‌های دکمه debounce شده
- `lock_status` - خروجی ('1' = باز)

**Generic**: `UNLOCK_TIME` - تأخیر قفل خودکار به سیکل کلاک

---

### button_debouncer.vhd

**هدف**: فیلتر نوسان مکانیکی، خروجی پالس‌های تک‌سیکل

**پورت‌ها**:
- `clk`, `reset` - کلاک و ریست
- `button_in` - ورودی خام دکمه
- `button_out` - خروجی تمیز و edge-detected

**Generic**: `DEBOUNCE_TIME` - دوره پایداری به سیکل کلاک

---

### top_level.vhd

**هدف**: سیستم کامل آماده برای سنتز

**پورت‌ها**:
- `clk`, `reset` - کلاک و ریست سیستم
- `button_A/B/C/D_raw` - ورودی‌های خام دکمه
- `lock_status`, `led` - نشانگرهای خروجی

**Generics**: `DEBOUNCE_TIME`, `UNLOCK_TIME`

---

## مستندات تفصیلی

دایرکتوری [docs/](../docs/) را برای مستندات جامع ببینید:

- [digital_lock.md](../docs/digital_lock.md) - معماری و طراحی FSM
- [button_debouncer.md](../docs/button_debouncer.md) - عملکرد debouncer
- [top_level.md](../docs/top_level.md) - راهنمای یکپارچه‌سازی سیستم

---

## استانداردهای طراحی

همه فایل‌های منبع از این قراردادها پیروی می‌کنند:

- استاندارد **VHDL-2008** (`--std=08`)
- **IEEE std_logic_1164** برای انواع منطقی
- **الگوی FSM سه‌پروسه** برای ماشین‌های حالت
- **پارامترهای generic** برای زمان‌بندی قابل تنظیم
- **طراحی همگام** با ریست ناهمگام
- **کاملاً قابل سنتز** - بدون ساختارهای فقط شبیه‌سازی
